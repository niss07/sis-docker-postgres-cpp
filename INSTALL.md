# INSTALL

## Requirements
- Git
- Docker & Docker Compose
- C++ compiler (g++)
- CMake
## Setup
Detailed setup will be added in Week 2.


## Docker (Week 2)

### Build C++ Application Image
```bash
docker build -t sis-cpp-app .
docker run --rm sis-cpp-app
---

## Docker (Week 2) — Student 3 (C++)

### Docker Desktop + WSL (Windows)
If you are on Windows, install **Docker Desktop** and enable **WSL 2**.  
If Docker shows “WSL needs updating”, run:
```bash
wsl --update



